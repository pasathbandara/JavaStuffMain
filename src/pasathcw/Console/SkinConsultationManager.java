package pasathcw.Console;

public interface SkinConsultationManager {
    void run(); /*the interface class can only hold method names as the bodies has to be written on another class a major use of an interface is multiple inheritance*/
    void deleteDoctor();
    void addNewDoctor();
    void printList();
    void saveFile();
}
